spring:
  data:
    redis:
      host: 10.0.0.6 # Redis服务器地址
      database: 0 # Redis数据库索引（默认为0）
      port: 6379 # Redis服务器连接端口
      password: redis_7JaMr2 # Redis服务器连接密码（默认为空）
      timeout: 5000ms # 连接超时时间（毫秒）

management:
  health:
    redis:
      enabled: false
