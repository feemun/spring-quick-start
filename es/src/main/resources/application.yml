# Elasticsearch 配置
elasticsearch:
  host: localhost
  port: 9200
  scheme: http
  username: elastic
  password: 123456
  connection-timeout: 5000
  socket-timeout: 30000

# 日志配置
logging:
  level:
    org.elasticsearch: DEBUG
    cloud.catfish.es: DEBUG